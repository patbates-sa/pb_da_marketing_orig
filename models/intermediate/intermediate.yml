version: 2

models:

  - name: int_engaged_users
    description: Intermediate model aggregating user engagement by user
    columns:
      - name: user_id
        description: Primary key - unique user identifier
        tests:
          - unique
          - not_null
      - name: total_engagement
        description: Sum of all engagement activity for the user


