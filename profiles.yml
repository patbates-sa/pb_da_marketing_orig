# =============================================================================
# dbt Profiles Configuration
# =============================================================================
#
# This file uses environment variables for sensitive data.
# See README.md for full setup instructions with copy-paste commands.
#
# QUICK REFERENCE
# ---------------
# You need to set 4 environment variables on your Mac. These are values that
# dbt reads automatically when you run commands.
#
# To set them up:
#   1. Open Terminal
#   2. Run this command to open your shell config file:
#        open -e ~/.zshrc
#   3. Add these 4 lines at the bottom (replace the values with your own):
#
#        export DBT_SNOWFLAKE_USER="your_snowflake_username"
#        export DBT_DEVELOPMENT_SCHEMA="dbt_yourname"
#        export DBT_SNOWFLAKE_PRIVATE_KEY_PATH="~/.ssh/snowflake_rsa_key.p8"
#        export DBT_SNOWFLAKE_PRIVATE_KEY_PASSPHRASE="your_passphrase"
#
#   4. Save (Cmd+S) and close the file
#   5. Run this in Terminal to apply the changes:
#        source ~/.zshrc
#   6. Test your connection:
#        dbt debug
#
# =============================================================================

tr995_us1_70437463662272:
  outputs:
    dev:
      type: snowflake
      threads: 16
      account: zna84829
      warehouse: TRANSFORMING_V2
      role: TRANSFORMER
      database: da_marketing

      # User-specific values from environment variables
      user: "{{ env_var('DBT_SNOWFLAKE_USER') }}"
      schema: "{{ env_var('DBT_DEVELOPMENT_SCHEMA') }}"
      private_key_path: "{{ env_var('DBT_SNOWFLAKE_PRIVATE_KEY_PATH') }}"
      private_key_passphrase: "{{ env_var('DBT_SNOWFLAKE_PRIVATE_KEY_PASSPHRASE') }}"

  target: dev
